- RIBs => 갈비 립 + Router / Interactor / Builder의 줄인 말로 해당 작은 모듈의 상하위 계층을 통한 아키텍쳐 형태를 RIBs 라고 함
- TODO Router의 기능은 아직 모호함. 쓰면서 익힐 예정
	- 기존의 내가 아는 라우터의 형태는 앱이 이동하거나 화면이 전환 되는 등, 화면간의 데이터 전송으로 인지 하지만 RIBs 패턴에서는 다른 형태로 사용 될 듯 싶음.
- TODO Interactor의 기능은 아직 모호함. 쓰면서 익힐 예정
- TODO Builder의 기능은 아직 모호함. 쓰면서 익힐 예정
	- 다만 Builder는 여기서 객체를 생성하고 즉, 프로퍼티를 생성하고 관리 protocol로 하여금 부정확한 실루엣인 형태에서 실제 객체를 선언하는 작업을 수행한다고 현재 인지.
-
- appDelegate 에서 최초의 부모 리블렛을 추가하기 위해서 AppRootBuilder를 빌드 한다음에. 해당 하는 결과값으로 최초 앱 실행 시 리블렛을 attach 해주게 되고, 빌더를 통해 생성된 AppRootInteractor의 didBecomeActive 함수는 뷰에서 viewDidLoad와 비슷한 형태를 함으로 그 해당하는 라우터에 attachTabs를 통해 앱의 시작 시 탭 뷰 화면을 보여주도록 설정 되어 있는 순서임.
-
- Interactor에서 UI를 업데이트 할 때에는 Present를 호출해야함.
-
- RIBs에서 새로운 친구를 추가하려면 리블렛 폼을 하나 생성하고, 상위 폼에서 빌더에서 먼저 선언을 해서 사용 준비를 함.
-
- FinanceHomeBuilder에서 하위 리블렛의 빌더를 가져옴
- -> 그러면 디펜던시에서 해당 컴포넌트를 쓰기 위해선 프로토콜이 필요함. 파이낸스 홈 컴포넌트가 새롭게 추가되는 디펜던시를 따라줘야함.
- 이후에 라우터에 가서 초기화를 위한 자식 빌더블을 초기화 해줘야 생성 시에 붙음. 추가를 하다 보면, 초기화 코드에 추가를 해줘야하고, 그에 따라서 해당 홈 Interactable에도 새롭게 붙이는 리블렛의 프로토콜이 추가 됨.
-
-
- RIBs 아키텍쳐의 장점으로는 하나의 묶음으로만이 아니라 뷰가 없는 립 기능이 가능하게 됨.
- 이를 viewless Riblet 뷰 리스 리블렛을 최상위에 두면
- 스위프트는 모던한 프로그래밍 패턴을 따르는데,
	- 변수들은 항상 사용 이전에 초기화 된다.
	- 배열 인덱스들은 범위를 벗어남 에러를 체크한다.
	- 정수들은 오버플로우를 체크한다.
	- 옵셔널 값은 `nil` 값이 명시적으로 컨트롤되는 것을 보장한다.
	- 메모리는 자동으로 관리된다.
	- 에러 핸들링은 예상치 못한 오류/실패로부터 복구를 시킨다.
-